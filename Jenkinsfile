@Library('shared-library') _
pipeline {
    agent any
    stages {
        stage('verify') {
            steps {
                helloDevOpsWorld(name: 'Pipeline Workshop Attendees')
            }
        }
    }
}
